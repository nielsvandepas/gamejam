export default class Floor {
	constructor() {
        this.properties = {
            x: 0,
            y: window.innerHeight - 40,
            width: window.innerWidth,
            height: 40,
            color: "green"
        }
    }

    collided(collider){

    }
}