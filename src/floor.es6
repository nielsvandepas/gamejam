export default class Floor {
	constructor() {
        this.properties = {
            x: 0,
            y: window.innerHeight - 100,
            width: window.innerWidth,
            height: 100,
            color: "green"
        }
    }

}